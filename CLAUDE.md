# Financial-Sentiment-Analysis — 工作准则 (Working Principles)

> 本文件是与 jack 协作的最高准则。**每次写代码前,先通读这三条铁律。** 不得违背。
> 它存在的意义之一就是"不靠记忆、不会忘"——上下文被压缩也丢不了。
> (三条铁律与协作约定源自 cute-agent 仓库的同名文件,为跨项目通用准则。)

---

## 三条铁律 (Three Standing Rules)

### 第一句 · 中心与质量
- **中心**:采集股票社区(东方财富股吧、雪球等)的舆情数据,为后续**投资者情绪分析
  (Investor Sentiment Analysis)** 打基础。当前阶段**只做数据采集与可行性调研,不做情绪分析**。
- 一切产出必须保证四项:**准确性 (Accuracy) · 可读性 (Readability) · 对方(导师)认可度
  (Recognition) · 真实性 (Authenticity)**。
- **准确与真实是第一需求,绝不为省事/省 token 牺牲代码质量。**
- **反复检查,杜绝幻觉**:
  - 数据必须是**真实抓取来的**,绝不用模型记忆编造帖子/数字/字段。
  - 采集到的每条数据都带 `url + crawl_time`,可回溯核对;拿不到的字段显式**留空**,不臆造。
  - 爬虫写完必须**实际跑一遍验证**(真请求、真解析、看真实样本),不靠"看起来对"。
  - 事实(采到的原始数据)与加工(清洗/分析)分清楚,不混为一谈。

### 第二句 · 节奏
- **慢慢来,一步一步,要稳、要好。** 不追求一次性做完。
- 先搭第一版,再**反复迭代升级**。每一台阶:做扎实 → 验给 jack 看 → 确认后再走下一步。
- **拿不准就问 jack,不自负。** jack 会用其他大模型交叉验证产出。

### 第三句 · 可回退 (Reversibility)
- 后续更新的**重要操作必须可回退 (be able to reverse)**。**绝不能毁掉上一版。**
- 做法:
  - 每完成一个台阶就 **git 提交一次**,commit message 清晰。
  - 大改动前,先确保上一版已提交 / 可恢复;**不直接覆盖能用的版本**。
  - 万一新版"崩盘",必须能**一键回到上一版**。

---

## 协作约定 (Conventions)
- **开发分支**:`claude/financial-sentiment-analysis-w29uv5`(所有开发与 push 都到这个分支)。
- **语言**:对 jack 输出中文;代码内英文术语 + 中文注释。
- **API Key / Cookie**:即便仓库为 private,**凭据仍从环境变量读取,不在源码里硬编码**;
  集中放配置文件便于轮换。**确认为 private 之前,绝不把任何 Key/Cookie push 到远端。**
- **爬虫礼貌与合规**:控制请求频率(带随机延时)、数据仅用于研究/课程用途、不影响目标站正常服务。

---

## 进度台阶 (Roadmap — 每阶独立可验证)

**第一阶段:数据采集与各平台爬取可行性(当前)**
1. ✅ 环境与连通性:云环境可访问各站,依赖就绪
2. ✅ 项目骨架:统一字段 / 频率控制 / CSV 落盘 / README / .gitignore
3. ✅ 东方财富股吧:爬虫跑通,真实样本(列表 160 条 + 带正文 20 条),正文抽取已修
4. 🟡 雪球:Playwright 过阿里云 WAF 原型已写,**待实跑取样**
5. ⬜ 其余平台遍历:新浪财经 / 同花顺 / 淘股吧 逐个验证 + 原型
6. 🟡 汇总可行性报告:`docs/feasibility_report.md`(随进展更新)

**后续阶段(先拿数据,分析在后)**
- 第二阶段:在结构化数据上做情绪分析(词典法起步 → FinBERT / LLM)
- 第三阶段:情绪与股价/成交量对比,验证情绪是否有信息量

> 详细可行性调研见 `docs/feasibility_report.md`;字段设计见 `docs/data_schema.md`。
